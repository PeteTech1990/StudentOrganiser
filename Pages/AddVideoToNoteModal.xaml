<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="StudentOrganiser.Pages.AddVideoToNoteModal">
    <Border Stroke="Black" StrokeThickness="2" HeightRequest="400" Margin="10">
        <Grid RowDefinitions="4*, *, *">
            <Border x:Name="MediaView" HeightRequest="250" Grid.Row="0" Margin="10">
                <toolkit:MediaElement x:Name="videoPlayer" HeightRequest="250" ShouldAutoPlay="False" ShouldShowPlaybackControls="true"/>
            </Border>
            <Grid ColumnDefinitions="*,*" Grid.Row="1">

                <Button x:Name="LocalVideo" Clicked="AddLocalVideo" Text="🎞" Grid.Column="0" HorizontalOptions="Center" VerticalOptions="Center" FontSize="Large"></Button>
                <Button x:Name="YoutubeVideo" Clicked="AddYoutubeVideo" Text="▶" Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Center" FontSize="Large"></Button>

            </Grid>

            <Button x:Name="SaveVideo" Clicked="SaveVideoFile" Text="Done"  Grid.Row="3" WidthRequest="100" HorizontalOptions="End" Margin="10" HeightRequest="50"></Button>
        </Grid>
    </Border>
</ContentPage>